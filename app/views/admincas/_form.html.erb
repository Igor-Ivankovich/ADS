<%= form_with(model: post) do |form| %>
  <% if adminca.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(adminca.errors.count, "error") %> prohibited this adminca from being saved:</h2>

      <ul>
        <% adminca.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
